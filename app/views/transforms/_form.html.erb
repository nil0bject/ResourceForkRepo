<%= form_for(@transform) do |f| %>
  <% if @transform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transform.errors.count, "error") %> prohibited this transform from being saved:</h2>

      <ul>
      <% @transform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_area :data %>
  </div>
  <div class="field">
    <%= f.label :content_type %><br>
    <%= f.text_field :content_type %>
  </div>
  <div class="field">
    <%= f.label :resource_id %><br>
    <%= f.text_field :resource_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
